const pageStyle = {
  padding: '2rem',
  textAlign: 'center',
  fontSize: '1.5rem',
  color: '#555',
};

const UploadDocument = () => {
  return (
    <div style={pageStyle}>
      <h2>Upload Document Page</h2>
      <p>Content for uploading documents will go here.</p>
    </div>
  );
};

export default UploadDocument;
