const pageStyle = {
  padding: '2rem',
  textAlign: 'center',
  fontSize: '1.5rem',
  color: '#555',
};

const SoftDelete = () => {
  return (
    <div style={pageStyle}>
      <h2>Soft Delete Page</h2>
      <p>Content for soft-deleting items will go here.</p>
    </div>
  );
};

export default SoftDelete;
