const pageStyle = {
  padding: '2rem',
  textAlign: 'center',
  fontSize: '1.5rem',
  color: '#555',
};

const UploadMetadata = () => {
  return (
    <div style={pageStyle}>
      <h2>Upload Metadata Page</h2>
      <p>Content for uploading metadata will go here.</p>
    </div>
  );
};

export default UploadMetadata;
